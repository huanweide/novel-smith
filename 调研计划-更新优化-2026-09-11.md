# Novel Smith 更新优化 · 深度调研与工作计划（工作项目）

> 基线版本：**v3.1.114** · 调研日期：2026-09-11 · 调研方式：代码级实测（非推断）
> 真身路径：`C:\Users\Administrator\WorkBuddy\2026-07-25-14-19-44\novel-forge`

---

## 零、先说两条调研纪律（本次严格执行）

本项目历史文档很多，但**老报告不能直接当待办源**。本次调研两条规矩：

1. **只认代码里当前还存在的问题**。凡是被后续迭代解决掉的，一律不写进计划。
2. **子代理 / 二手结论必须回源核实**。本次派出的调研代理给出了两条**错误结论**，已当场推翻（详见附录 A），如果直接采信会导致做无用功甚至改坏好代码。

> 教训价值很高，已写入项目长期记忆：**本项目是「薄 route + 胖 core」分层架构**，
> `src/app/api/**/route.ts` 只是壳，真正的业务逻辑在 `src/core/**`。
> 任何「某某约定采用率 0」的统计如果只扫了 route 层，一定得到假阴性。

---

## 一、现状硬指标（本次实测）

| 指标 | 实测值 | 备注 |
|---|---|---|
| 源码规模 | 639 文件 / 125,095 行 | 不含 `src/generated`（prisma 生成代码） |
| 页面路由 | 14 个 | |
| API 路由 | **137 个** | |
| 测试文件 | 155 个 / 1656 条断言 | 上一版 v3.1.114 新加 20 条 |
| 手写超大文件（>2000 行） | **仅 1 个** | 见下文 P0-1 |
| TODO / FIXME / HACK 残留 | **1 处** | 极干净 |
| 上一轮已完成 | 静态 a11y 回归守卫、6 套主题 WCAG 对比度收口 | 本次不重复 |

---

## 二、核心发现（按优先级）

### P0-1 · changelog 页面 DOM 爆炸（最明确的真问题）

**证据**
- `src/lib/changelog-data.ts`：**14,848 行，累积 539 个版本条目**（`VERSIONS` 数组）。
- `src/app/changelog/page.tsx:40`：`VERSIONS.map(...)` **全量渲染，无分页、无懒加载、无虚拟化**。
- 被引用方：`src/app/changelog/page.tsx:4`、`src/app/home-dashboard.tsx:6`、`src/app/api/health/route.ts`。

**影响**
- 每个版本含若干 `sections × items`，539 个版本展开后是**上万级 DOM 节点**。
- 后果不只是慢：屏幕阅读器要把整个节点树读一遍，**直接破坏上一轮刚补好的无障碍成果**；
  同时该数据文件一旦没被 tree-shake 干净，会整包进前端 bundle，拖慢首屏。
- 这是典型的「版本越迭代越卡」型债务——**现在不动，以后每次发版都在变坏**。

**做什么**
- 首屏只渲染最近 N 个版本（如 20 个），其余走「加载更早」按钮或滚动懒加载；
- 或按年份折叠，默认只展开当年；
- 顺带确认 `changelog-data.ts` 是否真的被 tree-shake（若没有，把 `VERSIONS` 拆到独立模块、`home-dashboard` 只引入一个极小的 `latest.ts`）。

**验收标准**
- `/changelog` 首屏 DOM 节点数下降一个数量级（实测前后对比）；
- `home-dashboard` 相关 chunk 体积不随版本数增长；
- 现有 539 个版本一条都不能丢，仍能全部回溯。

**预估**：0.5~1 天 · 无外部依赖 · 风险低

---

### P0-2 · 137 个 API 零鉴权（本地场景可接受，但需显式收口）

**证据**：`src/middleware.ts` **不存在**；137 个路由中做入站鉴权的为 **0**。
仅有的 5 处 `Authorization` 全是**出站**调 LLM 的请求头，不是入站校验。

**准确定性**：这是**有意的产品定位**（本地单机、数据不出本机），不是疏忽。
但项目里有 `deploy-local.ps1`——一旦被人部署到局域网或公网，就是任意读写。

**做什么**：加 `src/middleware.ts`，非回环地址（`127.0.0.1` / `::1`）一律 403；
本地完全无感，对外立刻具备底线防护。属一次投入、137 路由全覆盖。

**验收标准**：本地 `localhost:3001` 全部功能回归无感；用局域网 IP 访问被拒且提示明确。

**预估**：0.5 天 · 风险低 · 收益覆盖整个后端

---

### P1-1 · 组件层测试薄弱（最容易出错的地方最没人管）

**实测分布**

| 层 | 源文件 | 测试文件 | 判断 |
|---|---|---|---|
| `src/core` | 110 | 82 | 强 |
| `src/lib` | 43 | 36 | 强 |
| `src/components` | **130** | **18** | **明显薄弱** |

**为什么这条优先**：`components` 是直接面对作者的那一层，也是唯一能被 AI 之外的
人看见的部分。核心算法层写得再漂亮，按钮点错、边界态崩了，作者感知就是「这软件不行」。
上一轮 humanize 的一键套用能安全上线，靠的就是组件层测试锁住「没回调不许画按钮」。

**做什么**：优先给**有状态 + 有副作用**的组件补测试（Modal / Panel / 编辑器 / SSE 消费组件），
纯展示组件可后置。参考既有 `HumanizePanel.test.tsx` 的写法（守产品底线，而非快照测试）。

**预估**：持续项，每批加固 3~5 个

---

### P1-2 · `src/core/llm` 零测试（最烧钱、最容易静默出错的模块没网）

**证据**：`src/core/llm` 源文件 1 个，测试 **0 个**（`src/core` 下唯一零测试模块）。

**为什么危险**：这里是所有 LLM 调用的必经之路（Key 装配、Base URL 纠错、重试、降级）。
它出错的表现不是报错，而是**悄悄用了错的 endpoint / 悄悄烧掉额度**——
历史上「硅基流动填了却走 OpenAI 地址」那类事故就出在这一层。
上一轮 a11y 的经验已经证明：**把结论固化成测试才是真的闭环，注释和人工检查都不是**。

**做什么**：给 provider / baseUrl 解析、Key 有效性判断、错误分类补纯函数测试。

**预估**：0.5 天 · 纯收益

---

### P1-3 · 内部约定造好了却没落地（约定 ≠ 现实）

项目里早就建了统一的工具，`src/app/api` 层实际采用率：

| 工具 | 作用 | 采用（/137 路由） |
|---|---|---|
| `readValidatedBody`（`src/lib/validators.ts:76`） | 带 schema 的入参校验 | **6** |
| `safeJson`（`src/lib/api-body.ts:55`） | 防畸形 JSON 抛错 | **16** |
| `requireFields`（`src/lib/api-body.ts:23`） | 必填校验 | **3** |

对照：137 个路由中 **71 个直接 `request.json()`**。

**做什么**：不追求一步到位。优先替换**写操作 + 会调 LLM** 的路由（这两类出错代价最大），
读路由可渐进。原则是让工具成为默认路径，而不是靠每个写路由的人记得。

---

### P2 · 其他

- **console.log 残留 13 处**：分布在 `babylore/loop.ts`、`dissect/engine.ts`、`plan-chapter.ts` 等。
  多数是有意的运行日志（历史上多次专门治理过刷屏），**非阻塞**，可随模块迭代顺手清理。
- **危险操作**：`story/nodes/[id]/purge`（硬删）、`import/commit` 的全量 `deleteMany`
  缺少服务端二次确认。本地场景影响有限，建议在 P0-2 的中间件之后统一处理。

---

## 三、已核实「不是问题」的三条（防止误改好代码）

这几条容易被当成缺陷误报，明确排除：

1. **乐观锁 `assertNodeUnchanged`「采用率 0」——子代理误报**。
   实测在三个真正的写回点都有布防：`src/core/write-generation.ts:453`、
   `src/core/pipeline/post-processor.ts:112`、`src/core/game/game-engine.ts:917`。
   **不要**据此去「补乐观锁」，会重复加锁甚至引入冲突。
2. **限流默认关闭「是安全空洞」——误判**。
   `src/lib/rate-limit.ts:85` 注释与 `changelog-data.ts:7189` 都记录了这是**有意决策**：
   本地单机场景限流属过度防御，避免误伤本地作者。**保持现状**。
3. **错误信息泄露**——基本已收口，`jsonError` 采用率 114/137，
   仅 2 处 SSE 路由（`game/start`、`game/outline/chat`）残留，属 P2。

---

## 四、执行排期建议

| 批次 | 内容 | 目标 | 预估 |
|---|---|---|---|
| **批次 1** | P0-1 changelog 性能 + P0-2 鉴权中间件 | 消除最明确的两个真问题 | 1~1.5 天 |
| **批次 2** | P1-2 core/llm 测试 + 批次 1 补 = 进入 W3 | 让最危险的模块有网 | 0.5 天 |
| **批次 3** | P1-1 组件层测试加固（先挑 5 个重灾区） | 提升作者可感知质量 | 1~2 天 |
| **批次 4** | P1-3 约定落地 + P2 清理 | 长期可持续性 | 渐进 |

---

## 五、执行时必须守的项目铁律

1. **三道门禁**：`tsc --noEmit --incremental false` → `vitest run` → `next build`，全过才允许提交。
2. **bump 五件套**：`package.json` / `CHANGELOG.md` / `changelog-data.ts`（`LATEST_VERSION` +
   `CHANGELOG_BRIEF` + `VERSIONS`）/ `README.md` / `README_EN.md`，**逐条改、逐条 grep 验证**。
   （历史事故：同一文件并发 Edit 导致 `LATEST_VERSION` 静默丢失，发版后首页公告显示旧版本号。）
3. **提交前凭据扫描**：零命中才允许 push；临时脚本与日志一律不入库，只 `git add` 精确文件清单。
4. **推送走 SSH 远端 `ghssh`**，HTTPS 的 `origin` 已不可用；推完用 `git ls-remote` 核验一致。
5. **humanize 三条硬约束不可破**：纯本地不联网 / 只在作者点下按钮时才改（无自动改写）/ 说实话免责常显。

---

## 附录 A：本次调研推翻的两条子代理结论（教训）

| 子代理结论 | 实测真相 | 根因 |
|---|---|---|
| 「乐观锁 `assertNodeUnchanged` 采用率 0/137，正在发生静默数据丢失」 | 实际在 `core` 层三处写回点全部布防 | 只扫了 `src/app/api/**/route.ts`，没意识到本项目是**薄 route + 胖 core** 分层 |
| 「限流默认关闭 = 安全空洞」 | `rate-limit.ts:85` 与 changelog 记录都写明是**有意决策** | 把「有意取舍」当成「遗漏」 |

**结论**：二手调研结论用于排期可以，用于动手改代码之前**必须回源核实**。
本项目里「看起来没人用」的工具，很可能是被 core 层用着。
